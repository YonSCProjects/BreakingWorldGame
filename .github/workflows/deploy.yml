name: Deploy to GitHub Pages

# Builds the Vite PWA and publishes it to GitHub Pages on every push to main.
# The site is a project page served from /BreakingWorldGame/, so we build with
# that base path (overriding the repo's relative default) — this keeps asset
# URLs and the PWA service-worker scope correct on the subpath.

on:
  push:
    branches: [main]
  workflow_dispatch: # lets you re-run a deploy by hand from the Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

# Only one deploy at a time; a newer push supersedes an in-flight one.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build -- --base=/BreakingWorldGame/
      - uses: actions/configure-pages@v5
        with:
          # Try to turn Pages on automatically so there's no manual setup step.
          # If your org blocks this, enable it once: Settings → Pages → Source →
          # "GitHub Actions", then re-run this workflow.
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
